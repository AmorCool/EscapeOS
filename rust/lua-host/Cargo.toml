[package]
name = "lua-host"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]
path = "src/lib.rs"

[dependencies]
mlua = { version = "0.9", features = ["lua54", "vendored", "send"] }

[profile.release]
opt-level = "s"
lto = true
strip = true
